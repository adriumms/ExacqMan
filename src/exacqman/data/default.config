# ExacqMan default config (TOML).
#
# Authentication lives in a separate credentials file. Copy sample.credentials
# to default.credentials next to exacqman.py and fill in [auth]. Or change
# settings.credentials_file below / pass --credentials on the CLI for another path.
#
# Sections (TOML tables) are lowercase by convention. [settings] is reserved
# for program defaults; per-run values like server, camera, and time range come
# from CLI arguments. Every other top-level table is a server (e.g. [ch], [gpa])
# with a url. Cameras are sub-tables of their server ([<server>.<alias>]), so the
# same alias can be reused safely across different servers without ID
# collisions.
#
# All numeric values are real ints (no quotes); URLs and other strings
# require quotes. crop_dimensions are TOML arrays-of-arrays of ints:
# [[x, y], [width, height]].

[settings]
credentials_file = "default.credentials"
timelapse_multiplier = 50
# Global default compression: none | low | medium | high ("none" bypasses
# compression for native quality). A camera can override it with its own
# compression_level (see [mit.scragg-1] below); a --quality flag overrides both.
default_compression_level = "high"
timezone = "America/Indiana/Indianapolis"
# Overlay stroke weight on a 1-5 scale (1 = thinnest, 5 = heaviest).
font_weight = 2
default_crop = true
default_crop_dimensions = [[0, 0], [1920, 1080]]
port = 8887


[ch]
url = "http://10.20.4.12"

[gpa]
url = "http://10.60.4.10"

[mit]
url = "http://10.40.4.12"


[ch.dock-8]
id = 3483648
crop_dimensions = [[624, 14], [666, 766]]

[ch.dock-9]
id = 3483904
crop_dimensions = [[610, 12], [680, 798]]

[ch.dock-10]
id = 3484160
crop_dimensions = [[1312, 28], [1332, 1648]]

[ch.dock-11]
id = 3484416
crop_dimensions = [[650, 10], [666, 872]]

[ch.dock-12]
id = 3484672
crop_dimensions = [[616, 18], [688, 860]]

[ch.dock-13]
id = 3484928
crop_dimensions = [[550, 10], [768, 964]]

[ch.dock-14]
id = 3485184
crop_dimensions = [[586, 8], [702, 956]]

[ch.dock-15]
id = 3485440
crop_dimensions = [[594, 4], [712, 900]]


[gpa.dock-5]
id = 3476992
crop_dimensions = [[368, 2], [1082, 1070]]

[gpa.dock-6]
id = 3477248
crop_dimensions = [[6, 40], [904, 1038]]

[gpa.dock-8]
id = 3477504
crop_dimensions = [[838, 6], [1074, 1070]]


[mit.scragg-1]
id = 3502336
compression_level = "none"

[mit.scragg-2]
id = 3502080
compression_level = "none"

[mit.splitter]
id = 3500288
compression_level = "none"

[mit.splitter-exit]
id = 3502848
compression_level = "none"

[mit.splitter-infeed]
id = 3503104
compression_level = "none"

[mit.traffic-cop]
id = 3502592
compression_level = "none"

[mit.planer]
id = 3501824
compression_level = "none"